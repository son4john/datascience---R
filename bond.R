bondValue<-function(P,C,r,timeT,termC,freq){
  val = 0
  tmat = freq*(termC-timeT)
  accrued = C*freq*t
  if(tmat != 0){
    i
  }
  else{
    
  }
}
#add another function frequncey
#C*2*t 2 is semi-annual
#replace 2 with f
#cp <-P*C/freq C becomes a rate
#accrued P*C*t and c to C*P